{
  "id": "codex-updater",
  "name": "In-app Update button",
  "description": "Adds a small top-right Update button that appears when codex-update-manager reports a new upstream release. One click triggers the build (when needed) and restarts the app to install.",
  "defaultEnabled": true,
  "entrypoints": {
    "patches": "./patch.js",
    "stageHook": "./stage.sh"
  }
}
